apiVersion: v1
kind: Service
metadata:
  name: {{ include "aab-mongodb.fullname" . }}
  labels:
    {{- include "aab-mongodb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aab-mongodb.selectorLabels" . | nindent 4 }}
